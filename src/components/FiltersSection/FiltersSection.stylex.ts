import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  container: {
    display: 'flex',
    flexWrap: 'wrap',
    gap: '24px',
  },
  filtersContainer: {
    display: 'flex',
    flexWrap: 'wrap',
    gap: '12px',
  },
});

