import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  badgesContainer: {
    marginBottom: 12,
    marginTop: 4,
  },
  container: {
    marginBottom: 16,
  },
  rangeFiltersContainer: {
    marginTop: 10,
  },
  resetButton: {
    marginLeft: 12,
    padding: '6px 16px',
  },
});

